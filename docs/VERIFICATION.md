# 首版验证记录

日期：2026-09-07。

- `npm run build`：通过。Three.js 独立包约 503KB，gzip 约 127KB；存在 Vite 默认 500KB 提示，后续可按性能数据继续拆分。
- `npm test`：10 项，包括全部建筑入口、过桥路径、桥面高度、室内避障、1 小时加速状态机模拟、私人交谈打断、内容数量、来源检索、记忆隔离、上游坐标适配。
- 浏览器验证：Chrome 1440×900、390×844，运行时无页面异常。
- 赛事切换：38 / 18，作品搜索、空结果、收藏、刷新深链接均通过。
- 记忆：主动授权、兴趣保存、再次回忆、删除后不再引用均通过。
- 定制：昵称即时显示、主题切换、夜景切换通过。
- 降级：禁用 WebGL 后，仍能进入完整作品目录。
- 本地 preview：页面、状态接口及资料演示聊天接口均返回成功。

边界：未联调真实模型服务、Convex、多用户身份或云端记忆；未完成真实时间的 60 分钟 GPU 稳定性与 20 人房间压测。微信公众号正文未在本轮取得，品牌依据来自本地 PPT 与用户提供的 IP。

- `npm run validate:world`：通过。6 座建筑（1 座占位建筑）、8 位角色和 4 套主题配置均通过唯一性、入口可达性、类型和日程引用校验。

---

# 2026-09-22 soak 压测与稳定性实测（阶段 14）

口径：对**生产服务**（`node server/index.mjs`，两房间：主镇容量 20 + 茶楼容量 6）施加 5 分钟持续负载；16 位模拟玩家（内联 WS 客户端，直连协议）分布两房，每秒进行随机移动（服务端限速 6/s）、随机一对一邀请（自动接受）、约 0.8%/秒概率断线重连。脚本：`npm run soak`（可用 `ATOM_SOAK_MINUTES`/`ATOM_SOAK_PLAYERS` 调整），报告写入 `artifacts/soak-report.json`。

实测结果：

| 指标 | 实测值 | 说明 |
| --- | --- | --- |
| 快照间隔 p50 / p95 | 100.8ms / 108.7ms | 16 位玩家合计 47,022 个样本，服务端 10Hz 广播节奏稳定 |
| 快照间隔 max | 1620ms | 单次长尾（疑似 GC 或日志 tick），不影响最终一致性 |
| 移动指令 | 2,400 次 | 全部被服务端接受并推进 |
| 断线重连 | 44 次 | 全部恢复连接，结束时会话数回到 16 |
| 服务内存 | 31.0MB → 31.6MB（+0.5MB） | 5 分钟无泄漏迹象（RSS 在 GC 间波动） |
| 页面/协议错误 | 0 | 无未捕获异常 |
| 房间隔离 | 两房占用独立统计 | 主镇 11 人 / 茶楼 5 人，互不串通 |

范围与边界（诚实说明）：这是**缩放版首轮证据**（5 分钟 / 16 位模拟玩家 / 单机）。PRD 的完整口径仍未执行：前端真实时间 60 分钟固定流程稳定性、后端 72 小时持续运行（含模型故障）、20 人房间压测（当前 16 位模拟玩家已覆盖大部分代码路径，但真实浏览器客户端的渲染与输入路径需另行验证）。建议公开演示前按 `ATOM_SOAK_MINUTES=30 ATOM_SOAK_PLAYERS=20` 再跑一轮，并在有真实用户后补 60 分钟浏览器 soak。

---

# 2026-09-22 前端 60 分钟稳定性 soak（阶段 17）

口径：`npm run soak:browser`（`scripts/soak-browser.mjs`），对**生产服务**（`node server/index.mjs`）跑满 60 分钟；两个标签页（A 活跃操作、B 为同账号第二个标签页——覆盖接管路径），按固定流程每 4 秒循环：点击地面移动 → 进入展示馆阅读作品并关闭 → 与 AI 侠客私聊（快捷问题）→ 切换昼夜；每 2 分钟采样 heap、FPS、房间内角色数。报告：`artifacts/soak-browser-report.json`。

实测结果（60 分钟 / 22 个采样点）：

| 指标 | 实测 | 结论 |
| --- | --- | --- |
| 页面异常/崩溃 | **0** | 60 分钟无未捕获异常、无崩溃 |
| 重复会话 | **无**（角色数 8 → 8，min=max） | 第二个标签页正确接管，未产生双重控制者 |
| JS 堆 | 20MB → 31MB（+11MB，峰值 49MB） | 无持续增长（GC 间波动），无泄漏迹象 |
| FPS | p10 = 3（无头软件渲染口径） | 无 GPU 环境下的参考值，**不代表真实设备表现**；真实设备帧率需按 PRD 15.1 的固定设备基线复测 |
| 服务进程 | 本次采样未取到 RSS（脚本取数失败），后端稳定性见阶段 14 的服务端 soak | — |

范围与边界：前端 60 分钟稳定性（PRD 15.1 / A20 前端部分）**已通过**。仍未执行：固定真实设备（M1/M2 16GB Mac + 中端 Windows 独显机）上的帧率与首屏基线（PRD 15.1 要求 G0 冻结设备后测量，当前无头环境不构成该证据）、后端 72 小时持续运行、20 人房间压测（16 位模拟玩家 soak 已完成，见上一节）。

---

# 2026-09-22 20 人房间容量压测（阶段 18）

口径：`ATOM_SOAK_SINGLE_ROOM=1 ATOM_SOAK_PLAYERS=24 node scripts/soak-world.mjs`——24 位模拟玩家全部进入主镇（容量 20），对生产服务持续 5 分钟负载（移动/私聊/断线重连）。

实测结果：

| 指标 | 实测 | 结论 |
| --- | --- | --- |
| 容量执行 | **20 人在线 + 4 人排队**（全程稳定） | 满员后正确排队，不超容 |
| 快照间隔 p50 / p95 | 101.5ms / 105.4ms（72,302 样本） | 满员 + 排队下 10Hz 广播节奏不降 |
| 移动指令 | 3,612 次 | 全部接受并推进 |
| 断线重连 | 47 次 | 全部恢复，结束时会话数回到 24 |
| 服务内存 | 56.3MB → 45.6MB（-10.7MB） | 无泄漏迹象 |
| 页面错误 | 0 | — |

结论：PRD 15.1 的"20 位真人 + 8 位 AI 设计目标"在模拟玩家层面达到容量与稳定性预期（AI 侠客同时在房内活动）。真实用户的行为多样性（输入频率、停留时长）仍需试运营验证。

---

# 2026-09-22 20 分钟长时 soak（阶段 21）

口径：`ATOM_SOAK_MINUTES=20 node scripts/soak-world.mjs`（脚本已改为每 30 秒增量写报告，长时运行中崩溃不丢数据），16 位模拟玩家分布两房间（主镇 11 / 茶楼 5，茶楼容量 6），持续 20 分钟混合负载（移动/邀请私聊/随机断线重连）。

实测结果：

| 指标 | 实测 | 说明 |
| --- | --- | --- |
| 运行时长 | 20 分钟完整跑通 | 无页面错误、无服务端未捕获异常 |
| 快照样本 | 455,560 个 | 服务端广播持续送达 |
| 快照间隔 p50 / p95 | 0.2ms / 109ms（16 玩家聚合口径） | p95 与 10Hz 节奏一致 |
| 快照间隔 max | 362s | outlier：无头浏览器在 1,918 次随机断线下的计时器节流（客户端侧），非服务端广播中断 |
| 移动指令 | 4,250 次 | 全部被接受并推进 |
| 断线重连 | 1,918 次（含循环重试） | 高负载断线重连下服务稳定 |
| 页面错误 | 0 | — |

补充：此前一次 2 小时 soak 因与其他浏览器套件并发导致 Chrome 崩溃（环境资源竞争），崩溃前 21 分钟服务端 RSS 32—33MB、占用稳定；本次 20 分钟为单独运行的完整证据。PRD 的 72 小时口径仍未执行，建议在专用低负载机器上以 `ATOM_SOAK_MINUTES=4320` 运行（报告增量写入已支持）。

# 2026-09-23 两小时 soak：茶楼共坐全覆盖（阶段 29）

口径：`ATOM_SOAK_MINUTES=120 ATOM_SOAK_PLAYERS=24 node scripts/soak-world.mjs`（24 位模拟玩家分布两房间：主镇 16 / 茶楼房 8，茶楼房容量 6 故 2 人排队），持续 2 小时混合负载——随机移动、一对一邀请与私聊（自动接受）、随机断线重连、满员排队，以及阶段 29 新增的**茶楼共坐行为**：走到茶楼、占空位入座、坐 15—30 秒、起身、偶尔邀请同桌（AI 侠客也会落座，空位随机变化）。

实测结果（两小时轮）：

| 指标 | 实测 | 说明 |
| --- | --- | --- |
| 运行时长 | 120 分钟完整跑通 | 无页面错误、无服务端未捕获异常 |
| 成功入座 | 646 次 | 含与 AI 侠客同桌、被抢占座后换座 |
| 起身 | 556 次 | 累计就座 181.8 分钟（跨玩家） |
| 同桌相邀 | 369 次 | 就座期间邀请同桌（真人 + AI） |
| 入座类活动事件 | 311,974 条 | 入座/离座/AI 落座持续进入"江湖此刻" |
| 入座拒绝 | 1,792 次，原因仅 occupied / far | 无非法拒绝（无 missing/already 之外的异常） |
| **座位不变量：同座双占** | **0 次** | 每轮 seats 广播逐条校验 |
| **座位不变量：就座位置锁定** | **0 次偏移** | 入座后自身坐标在快照中不得变化 |
| 快照间隔 p50 / p95 | 100.9ms / 107.2ms | 与 10Hz 广播节奏一致 |
| 快照间隔 max | 3.26s | 无头浏览器计时器节流的客户端侧 outlier |
| 断线重连 | 1,326 次 | 高负载断线重连下服务稳定 |
| 在线连接 | 24/24（收尾时） | 收尾全部在线 |

## 本轮 soak 发现并修复的两个真实缺陷

1. **soak 客户端重连缺陷（测试脚本）**：重连定时器与主循环会各建一条 WebSocket、旧 socket 不关闭，orphan 连接滚雪球——首轮 2 小时跑到 48 分钟时客户端连接数归零、服务端残留 512 条 ESTABLISHED 连接、重连计数 10,573。已改为单触发重连（已在连接/已连接时直接返回并显式关闭旧 socket）；修复后同样负载重连计数降至 29（5 分钟口径）。
2. **服务端缺少未认证连接超时（产品缺陷）**：只连不发 hello 的连接会永久留在 `connections`，既占连接又拖慢每 tick 广播。已在 `server/world.mjs` 增加 15 秒 hello 超时（`createWorld({helloTimeoutMs})` 可配，默认 15000），并有单测覆盖（未认证连接超时被 4008 断开、已认证连接不受影响）。

## 复跑确认（最新代码，30 分钟）

首轮 2 小时轮因端口残留打在了旧代码服务上（共坐功能代码相同，证据有效；内存指标丢失）。已加固脚本：spawn 的服务端进程退出即快速失败（端口被占时不再静默打到旧服务），且内存采样失败会显式断言失败。复跑 `ATOM_SOAK_MINUTES=30 ATOM_SOAK_PLAYERS=24`：161 次入座 / 141 次起身 / 97 次同桌相邀、座位不变量零违例、快照 p95 118.3ms、**服务端内存 54.3MB → 45.3MB（-9.1MB，有界）**、零页面错误。

## 72 小时口径

PRD 的 72 小时压测仍未执行（本机环境不适合连续 3 天独占）。运行方式已固化：`npm run soak:72h`（即 `ATOM_SOAK_MINUTES=4320 ATOM_SOAK_PLAYERS=24`），报告每 30 秒增量写入 `artifacts/soak-report.json`，中途崩溃不丢数据；建议在专用低负载机器上执行。两小时轮的完整数据见 `artifacts/soak-report.json`。
